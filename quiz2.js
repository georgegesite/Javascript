function main() 
{
    var depth = parseInt(readLine(), 10);
    //your code goes here
    var copy_depth = depth;
    const up = 7;
    const down = 2;
    let days = 0;
    let counter = 0;
    
   for(i = 0; i < copy_depth +5; i++){
       if(counter => depth)
       {
           break;
       }
      counter = up - down;
      days++;
   }

   console.log(days);
}